'use client';

import { useState } from 'react';

const OUTCOMES = ['supports', 'refutes', 'qualifies', 'inconclusive'];
const RELATIONS = ['independent', 'partially_independent', 'same_implementation', 'unknown'];
const SEVERITIES = ['critical', 'major', 'warning', 'note'];

export function VerificationReceiptForm() {
  const [form, setForm] = useState({ receiptId: '', claimId: '', claimRevision: '1', runId: '', contractId: '', contractRevision: '1', outcome: 'supports', implementationRelation: 'independent', dataRelation: 'independent', findingId: '', findingSeverity: 'note', findingCode: '', findingDetails: '', fieldPath: '', source: '', excerpt: '' });
  const [preview, setPreview] = useState(null);
  const [error, setError] = useState(null);
  const update = (key) => (event) => setForm((current) => ({ ...current, [key]: event.target.value }));
  function prepare(event) {
    event.preventDefault(); setError(null);
    try {
      if (Object.values(form).slice(0, 10).some((value) => value === '')) throw new Error('All receipt identity and independence fields are required.');
      if (!form.findingId || !form.findingCode || !form.fieldPath) throw new Error('Finding ID, code, and field path are required for a located Finding.');
      const finding = { findingId: form.findingId, severity: form.findingSeverity, code: form.findingCode, details: { text: form.findingDetails, location: { fieldPath: form.fieldPath, source: form.source, excerpt: form.excerpt } } };
      setPreview({ ...form, claimRevision: Number(form.claimRevision), contractRevision: Number(form.contractRevision), findings: [finding] });
    } catch (reason) { setError(reason.message); setPreview(null); }
  }
  return <form aria-label="Verification Receipt form" onSubmit={prepare} className="mt-8 grid gap-4 rounded-xl border border-border bg-card p-6 md:grid-cols-2"><div className="md:col-span-2"><h2 className="text-2xl font-semibold">Verification Receipt</h2><p className="mt-2 text-sm text-muted-foreground">Record an outcome, independence assessment, and a Finding that can point back to an input field.</p></div>{[['receiptId', 'Receipt ID'], ['claimId', 'Claim ID'], ['runId', 'Run ID'], ['contractId', 'Contract ID']].map(([key, label]) => <label className="grid gap-2 text-sm font-medium" key={key}>{label}<input required className="rounded border border-input bg-background p-3" value={form[key]} onChange={update(key)} /></label>)}{[['claimRevision', 'Claim revision'], ['contractRevision', 'Contract revision']].map(([key, label]) => <label className="grid gap-2 text-sm font-medium" key={key}>{label}<input required min="1" type="number" className="rounded border border-input bg-background p-3" value={form[key]} onChange={update(key)} /></label>)}<label className="grid gap-2 text-sm font-medium">Outcome<select className="rounded border border-input bg-background p-3" value={form.outcome} onChange={update('outcome')}>{OUTCOMES.map((outcome) => <option key={outcome}>{outcome}</option>)}</select></label><label className="grid gap-2 text-sm font-medium">Implementation independence<select className="rounded border border-input bg-background p-3" value={form.implementationRelation} onChange={update('implementationRelation')}>{RELATIONS.map((relation) => <option key={relation}>{relation}</option>)}</select></label><label className="grid gap-2 text-sm font-medium">Data independence<select className="rounded border border-input bg-background p-3" value={form.dataRelation} onChange={update('dataRelation')}>{RELATIONS.map((relation) => <option key={relation}>{relation}</option>)}</select></label><fieldset className="grid gap-4 rounded-lg border border-border p-4 md:col-span-2"><legend className="px-2 text-sm font-semibold">Located Finding</legend><div className="grid gap-4 md:grid-cols-3"><label className="grid gap-2 text-sm font-medium">Finding ID<input required className="rounded border border-input bg-background p-3" value={form.findingId} onChange={update('findingId')} /></label><label className="grid gap-2 text-sm font-medium">Severity<select className="rounded border border-input bg-background p-3" value={form.findingSeverity} onChange={update('findingSeverity')}>{SEVERITIES.map((severity) => <option key={severity}>{severity}</option>)}</select></label><label className="grid gap-2 text-sm font-medium">Code<input required className="rounded border border-input bg-background p-3" value={form.findingCode} onChange={update('findingCode')} /></label></div><label className="grid gap-2 text-sm font-medium">Field path<input required placeholder="inputs.dataset.columns[3]" className="rounded border border-input bg-background p-3 font-mono text-sm" value={form.fieldPath} onChange={update('fieldPath')} /></label><div className="grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-medium">Source<input placeholder="run.stdout or artifact.json" className="rounded border border-input bg-background p-3" value={form.source} onChange={update('source')} /></label><label className="grid gap-2 text-sm font-medium">Excerpt<input placeholder="Relevant value or line" className="rounded border border-input bg-background p-3" value={form.excerpt} onChange={update('excerpt')} /></label></div><label className="grid gap-2 text-sm font-medium">Finding details<textarea className="min-h-20 rounded border border-input bg-background p-3" value={form.findingDetails} onChange={update('findingDetails')} /></label></fieldset><button className="w-fit rounded-md bg-primary px-4 py-2 text-primary-foreground" type="submit">Preview Receipt</button>{error && <p role="alert" className="text-sm text-destructive md:col-span-2">{error}</p>}{preview && <section aria-label="Verification Receipt preview" className="md:col-span-2 rounded-xl border border-primary/30 bg-primary/5 p-5"><h3 className="text-xl font-semibold">Receipt preview</h3><pre className="mt-3 overflow-x-auto rounded bg-muted p-4 text-xs">{JSON.stringify(preview, null, 2)}</pre></section>}</form>;
}
