import { SectionPlaceholder } from '@/components/section-placeholder';

export default function TasksPage() {
  return <SectionPlaceholder eyebrow="Work queue" title="Tasks" description="Find bounded research work, see what is blocked, and contribute with a clear next step." />;
}
