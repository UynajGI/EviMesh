/* Canonical public researcher URL from the M13.7 route contract. Keep the
 * older /contributors/:actorId route alive for existing permalinks. */
export { default } from '@/app/contributors/[actorId]/page';
