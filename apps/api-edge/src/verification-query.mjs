export class VerificationQueryError extends Error { constructor(message,code="VERIFICATION_QUERY_INVALID",status=400){super(message);this.name="VerificationQueryError";this.code=code;this.status=status;} }
export async function getVerificationReceipt({repository,receiptId}={}){if(typeof receiptId!=="string"||!receiptId.trim())throw new VerificationQueryError("receipt id must be a non-empty string");if(!repository||typeof repository.getVerificationReceipt!=="function"||typeof repository.listVerificationFindings!=="function")throw new VerificationQueryError("repository verification query methods are required");receiptId=receiptId.trim();const receipt=await repository.getVerificationReceipt(receiptId);if(!receipt)throw new VerificationQueryError("verification receipt not found","VERIFICATION_RECEIPT_NOT_FOUND",404);const findings=await repository.listVerificationFindings(receiptId);return {receipt,findings:Array.isArray(findings)?findings:[]};}
